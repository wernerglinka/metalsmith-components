{
  "name": "audio-playlist-player",
  "type": "_partials",
  "styles": ["audio-playlist-player.css"],
  "scripts": ["audio-playlist-player.js"],
  "requires": ["image", "icon", "video"],
  "validation": {
    "required": ["playlistName"],
    "properties": {
      "playlistName": {
        "type": "string",
        "description": "Name of the playlist JSON file (without extension)"
      },
      "options.showTrackList": {
        "type": "boolean",
        "default": true,
        "description": "Whether to display the track list"
      },
      "options.autoplay": {
        "type": "boolean", 
        "default": false,
        "description": "Whether to autoplay the first track"
      }
    }
  }
}